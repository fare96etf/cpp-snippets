#include "libs/data_structures.hpp"


int main(int, char**) {
    try_vectors();
    try_deque();
    try_array();
    try_list_and_forwardList();
    try_setmap_multisetmap_unorderedsetmap();
    try_stack_queue_priorityqueue();

    return 0;
}